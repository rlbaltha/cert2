<form class="d-inline float-right" method="post" action="{{ path('major_delete', {'id': major.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ major.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
