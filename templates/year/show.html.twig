{% extends 'base.html.twig' %}

{% block title %}Year - {{ parent() }}{% endblock %}

{% block body %}
    <h1>Year</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ year.id }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ year.year }}</td>
            </tr>
            <tr>
                <th>Display</th>
                <td>{{ year.display }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('year_index') }}">back to list</a>

    <a href="{{ path('year_edit', {'id': year.id}) }}">edit</a>

    {{ include('year/_delete_form.html.twig') }}
{% endblock %}
