<form method="post" action="{{ path('substitution_delete', {'id': substitution.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ substitution.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
